enum Direction {
    LEFT = 1
    RIGHT = 2
}

const set<i32> DIRECTIONS = [
    Direction.LEFT,
    Direction.RIGHT,
    Direction.LEFT,
    Direction.RIGHT,
];

const list<i32> DIRECTIONS_LIST = [
    Direction.LEFT,
    Direction.RIGHT,
    Direction.LEFT,
    Direction.RIGHT,
];